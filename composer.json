{
  "name": "dave-liddament/test-splitter",
  "description": "Splits up PHPUnit tests so they can be ran in parallel (e.g. on GitHub actions)",
  "type": "project",
  "require": {
    "php": "8.1.*|8.2.*|8.3.*|8.4.*|8.5.*"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.5.55",
    "php-parallel-lint/php-parallel-lint": "^1.4",
    "friendsofphp/php-cs-fixer": "^3.87.2",
    "phpstan/phpstan": "^2.1",
    "phpstan/phpstan-phpunit": "^2.0",
    "phpstan/phpstan-strict-rules": "^2.0"
  },
  "license": "MIT",
  "autoload": {
    "psr-4": {
      "DaveLiddament\\TestSplitter\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "DaveLiddament\\TestSplitter\\Tests\\": "tests/"
    }
  },
  "authors": [
    {
      "name": "Dave Liddament",
      "email": "dave@lampbristol.com"
    }
  ],
  "bin": [
    "tsplit"
  ],
  "scripts": {
    "ci" : [
      "@composer-validate",
      "@cs",
      "@lint",
      "@test",
      "@phpstan"
    ],
    "composer-validate" : "@composer validate --no-check-all --strict",
    "lint" : "parallel-lint src tests",
    "cs" : "PHP_CS_FIXER_IGNORE_ENV=1 php-cs-fixer fix --verbose --dry-run",
    "cs-fix" : "PHP_CS_FIXER_IGNORE_ENV=1 php-cs-fixer fix --verbose",
    "phpstan" : "phpstan analyse",
    "test" : "phpunit"
  }
}
